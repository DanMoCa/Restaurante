<h1>Listing platillos</h1>
<div class="datagrid">
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Precio</th>
      <th>Descripcion</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @platillos.each do |platillo| %>
      <tr>
        <td><%= platillo.nombre %></td>
        <td><%= platillo.precio %></td>
        <td><%= platillo.descripcion %></td>
         <td><%= platillo.categoria %></td>
        <td><%= link_to 'Show', platillo %></td>
        <td><%= link_to 'Edit', edit_platillo_path(platillo) %></td>
        <td><%= link_to 'Destroy', platillo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<br>


<%= link_to image_tag("/imagenes/new.jpg", :border => 0, :alt => '', :title => ''), new_platillo_path, :class => 'dock-item' %>